#include<iostream.h>
#include<conio.h>
int main()
{
 char a;
 cout<<"Enter an alphabet:";
 cin>>a;

 if(a=='a' || a=='i' || a=='e' || a=='o' || a=='u'|| a=='A' || a=='E' || a=='I' ||a=='O' ||a=='U')
  {
   cout<<"Enter alphabet is a vowel";
  }
 else
  {
   cout<<"Enter alphabet is a constent";
  }
}
